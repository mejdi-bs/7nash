[phases.setup]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx prisma db push --skip-generate --accept-data-loss 2>/dev/null || true && npm run start"

[variables]
NODE_ENV = "production"
